<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
        <div class=" navbar-collapse " id="navbarSupportedContent">
            <ul class="navbar-nav me-auto">
                <li class="nav-item">
                    <a class="nav-link fa-solid fa-house d-inline-block" href="{{ path('app_main') }}"></a>
                </li>
                {% if is_granted("IS_AUTHENTICATED_FULLY") %}
                    <li class="nav-item">
                        <a class="nav-link -danger"> Bonjour {{ app.user.contactDetail.lastname }}</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('app_user',{'user':app.user.id})}}">Voir vos demandes de contacts</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('app_logout')}}">Déconnexion</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('app_register')}}"> Créer un compte</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('app_login')}}">Connexion</a>
                    </li>
                {% endif %}
            </ul>
            <ul class="navbar-nav ms-auto">
                {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_contact_form') }}">Gestion des formulaires</a>
                    </li>
                {% endif %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_contactform_new') }}">Contactez-nous</a>
                </li>
            </ul>
        </div>
    </div>
</nav>